<div class="work_header">
    <img src="<%= "/#{@works.work_top_image} "%>">
</div>

        <ul class ="work_chara_title">
            <li>
                <img src="<%= "/#{@works.work_ico} "%>">
            </li>
            <li>
            </li>
        </ul>

<ul class ="main_layout">
    <li class = "side_menu">
        <ul class ="side_menu_list">
            <%= link_to("/works/#{params[:id]}") do %>
              <li>企画トップ</li>
            <% end %>
            <%= link_to("/works/#{params[:id]}/chara") do %>
              <li>キャラクター</li>
            <% end %>
            <%= link_to("/works/#{params[:id]}/invite_member_form") do %>
              <li>メンバー追加</li>
            <% end %>
          <li>脚本</li>
          <li>スケジュール</li>
          <li>タスク</li>
            <%= link_to("/users/#{@current_user.id}") do %>
              <li>マイページ</li>
            <% end %>
        </ul>
    </li>
    <li class ="main_content">
        <%= form_tag("/works/#{params[:id]}/const_section") do %>
            <h3>一括作成</h3>
            <input name="start_section" value = 1>話～<input name="last_section">話まで
            <input type="submit" value="作成">
        <% end %>
        
        <%= form_tag("/works/#{params[:id]}/add_section") do %>
            <h3>追加作成</h3>
            セクション名：<input name="section_name">
            <input type="submit" value="追加">
        <% end %>
    </li>
</ul>